# rand_isopeps dependencies.
#
# Preferred install (editable, src/ layout):  pip install -e .
#   extras:  pip install -e ".[quimb]"        # real isoTNS Moses Move + TEBD2
#            pip install -e ".[riemannian]"    # pymanopt disentangler
#            pip install -e ".[test]"          # pytest
# See pyproject.toml for the authoritative dependency declarations.
#
# Core (rand_isopeps package + synthetic-kernel experiments exp01-14):
numpy
scipy
matplotlib
threadpoolctl

# Optional extras (install as needed):
#   pip install pymanopt        # Riemannian disentangler (rand_isopeps.moses.disentangler)
#   pip install quimb autoray   # real isoTNS Moses Move + TEBD2 (rand_isopeps.real_isotns.*)
#
# Vendored SRC (src/rand_isopeps/compression/randommpomps/) needs only numpy; its
# optional C++ incremental-QR builds via
# src/rand_isopeps/compression/randommpomps/build_incrementalqr.sh.
