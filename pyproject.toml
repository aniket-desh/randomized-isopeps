[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "rand-isopeps"
version = "0.1.0"
description = "Physics-first imaginary-time isoPEPS with rMPS-sketched column moves"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "threadpoolctl",
]

[project.optional-dependencies]
# real quimb isoTNS Moses Move + TEBD2 (rand_isopeps.real_isotns.*)
quimb = ["quimb", "autoray>=0.9.0"]
# riemannian-renyi moses disentangler
riemannian = ["pymanopt"]
gpu = ["cupy-cuda12x"]
# convenience: everything
all = ["quimb", "autoray>=0.9.0", "pymanopt", "cupy-cuda12x"]
test = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
# keep the optional vendored C++ incremental-QR sources alongside the package
"rand_isopeps.compression.randommpomps" = ["*.cpp", "*.sh", "*.h"]

[tool.pytest.ini_options]
testpaths = ["tests"]
